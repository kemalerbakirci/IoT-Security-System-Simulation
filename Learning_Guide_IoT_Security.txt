# Learning Guide: IoT, MQTT, Node-RED, and Security System Simulation

## 1️⃣ What is IoT?
IoT (Internet of Things) connects physical devices (sensors, actuators) to the internet to collect and exchange data.

---

## 2️⃣ What is MQTT?
- A lightweight messaging protocol for IoT.
- Uses **Publish/Subscribe model**.
- Example: Door sensor publishes data, Node-RED subscribes to display it.

---

## 3️⃣ Key MQTT Concepts
- **Topics:** Channels where messages are published (e.g., home/security/door).
- **QoS:** Message delivery guarantee levels (0, 1, 2).
- **Persistent Sessions:** Broker remembers subscriptions if `clean_session=False`.

---

## 4️⃣ Node-RED Basics
- Visual tool to connect devices, APIs, and services.
- Dashboard nodes allow easy UI creation.
- MQTT nodes enable communication with IoT devices.

---

## 5️⃣ Python Libraries Used
- `paho-mqtt` → MQTT client
- `dotenv` → Load broker settings from `.env` file

---

## 6️⃣ Learning Path
✅ Learn Python basics  
✅ Learn JSON basics (`json.dumps`, `json.loads`)  
✅ Learn MQTT publish/subscribe using paho-mqtt  
✅ Install and run Mosquitto broker  
✅ Install Node-RED and create dashboard flows  

---

## 📚 Resources
- MQTT Essentials: https://www.hivemq.com/mqtt-essentials/
- Paho MQTT Docs: https://pypi.org/project/paho-mqtt/
- Node-RED Docs: https://nodered.org/docs/
